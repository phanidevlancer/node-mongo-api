module.exports = {
    testEnvironment: 'node',
    verbose: true,
    // Set timeout to 10 seconds
    testTimeout: 10000,
    // Ignore node_modules
    testPathIgnorePatterns: ['/node_modules/'],
};